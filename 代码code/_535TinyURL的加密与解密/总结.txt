方法1--问题懂题意之后很简单，需要注意string容器中的几个函数
1.string::rfind（）：是从字符串右侧开始匹配str，并返回在字符串中的下标位置
2.stoi（）：n进制字符串转十进制（stoi就是string to int 的缩写）
	            stoi(字符串，起始位置，几进制)；
	            stoi(str),将数字字符转化位int输出;
3.string::substr():例子string sub2 = s.substr(5, 3); //从下标为5开始截取长度为3位：sub2 = “567”
方法2--使用哈希生成，问题较为复杂，考虑到冲突处理
方法3--随机生成唯一数（时间不确定，变化大）